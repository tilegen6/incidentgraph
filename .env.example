PORT=4100
HOST=127.0.0.1
WEB_ORIGIN=http://localhost:3100
API_URL=http://localhost:4100
STORAGE_MODE=demo
DEMO_MODE=false
ADMIN_EMAIL=admin@incidentgraph.local
# Run npm run setup to generate a unique local password. Never commit it.
ADMIN_PASSWORD=
# Required only for PostgreSQL mode; keep the actual URL in an untracked .env.
# DATABASE_URL=
# REDIS_URL=
