# Screenshots

The browser verification suite can save `overview.png`, `investigation.png`, and `landing.png` here. These are local screenshots of the running project, not UI mockups. Replace them after changing the visual design or demo scenario.
